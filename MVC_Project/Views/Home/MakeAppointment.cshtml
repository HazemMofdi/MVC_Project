@model MVC_Project.ViewModels.SessionAppointmentViewModel
<style>
    .main {
        min-height: 80vh;
    }

    .make-apt-btn {
        padding: 5px 10px;
        font-size: 14px;
        border: none;
        margin: 80px 0 ;
        transition: 0.3s;
        background-color: #1977cc;
        color: white;
    }

        .make-apt-btn:hover {
            background-color: #1977ccc2;
            color: white;
        }
</style>
<section id="appointment" class="appointment section">
    <div class="container section-title aos-init aos-animate" data-aos="fade-up">
        <h2>Appointment</h2>
    </div>

    <div class="container aos-init aos-animate" data-aos="fade-up" data-aos-delay="100">
        <form asp-action="session" asp-controller="Session" method="post">

            <div class="row">
                <div class="col-md-4 form-group mt-3">
                    <input asp-for="Date" type="datetime-local" class="form-control" required step="3600" />
                </div>
                <div class="col-md-4 form-group mt-3">
                    <select asp-for="SessionType" class="form-select" required>
                        <option value="">Select Session Type</option>
                        <option value="VideoCall">Video Call</option>
                        <option value="Chat">Chat</option>
                    </select>
                </div>
                <div class="col-md-4 form-group mt-3">
                    <select asp-for="TherapistID" class="form-select" required>
                        <option value="">Select Doctor</option>
                        <option value="1">Hazem Mofdi</option>
                        <option value="2">Hassan Atef</option>
                        <option value="3">Rahma Tarek</option>
                        <option value="4">Nancy Saad</option>
                        <option value="5">Hamss Mohammed</option>
                    </select>

                </div>
            </div>

            <div class="form-group mt-3">
                <textarea asp-for="Message" class="form-control" rows="5" placeholder="Message (Optional)"></textarea>
            </div>

            <div class="text-center mt-3">
                <button class="make-apt-btn" type="submit">Make an Appointment</button>
            </div>
        </form>
    </div>
</section>


<script>
    document.addEventListener("DOMContentLoaded", function () {
        var dateInput = document.getElementById("date");

        function setMinDateTime() {
            var now = new Date();
            now.setMinutes(0);
            now.setSeconds(0);
            now.setMilliseconds(0);

            var localDatetime = now.toISOString().slice(0,16);
            dateInput.min = localDatetime;
        }

        setMinDateTime();
    });
</script>

